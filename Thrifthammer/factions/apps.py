"""App configuration for the factions app."""

from django.apps import AppConfig


class FactionsConfig(AppConfig):
    """Faction landing pages for SEO and price discovery."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'factions'
