[build]
builder = "nixpacks"

# Note: Railway cron jobs must be configured as separate services in the
# Railway dashboard (Settings → Cron Schedule), not in railway.toml.
# Scheduled tasks are handled via GitHub Actions:
#   .github/workflows/update-ebay-prices.yml  — daily 03:00 UTC
