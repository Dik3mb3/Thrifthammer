# Local Claude Code settings (may contain API keys)
.claude/settings.local.json
.claude/worktrees/

# Local database
db.sqlite3
*.db

# Loose scripts / scratch files at repo root (leading / = root only, not subdirs)
/*.py
/*.js
/*.txt
/*.json
!parser_output/**

# Virtual environments
venv/
.venv/

# Local production secrets / helper scripts (never commit these)
.env.production
/*.ps1
